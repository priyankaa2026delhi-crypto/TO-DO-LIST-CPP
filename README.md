# To-Do List App (C++)

A console-based to-do list manager built in C++.

## Features
- Add tasks
- View all tasks with status (Pending/Done)
- Mark tasks as done
- Delete tasks
- Persistent storage (tasks saved to a file, survive program restarts)

## How to run
g++ main.cpp -o main
./main
